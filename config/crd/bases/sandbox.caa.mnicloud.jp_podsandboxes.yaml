---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: podsandboxes.sandbox.caa.mnicloud.jp
spec:
  group: sandbox.caa.mnicloud.jp
  names:
    kind: PodSandbox
    listKind: PodSandboxList
    plural: podsandboxes
    shortNames:
    - psb
    singular: podsandbox
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.workloadRef.name
      name: Workload
      type: string
    - jsonPath: .spec.workloadRef.namespace
      name: Workload Namespace
      priority: 1
      type: string
    - jsonPath: .spec.className
      name: Class
      type: string
    - jsonPath: .spec.vcpus
      name: vCPU
      priority: 1
      type: integer
    - jsonPath: .spec.memoryMiB
      name: Memory MiB
      priority: 1
      type: integer
    - jsonPath: .status.nodeName
      name: Node
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.ips[0]
      name: IP
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodSandbox is the Schema for the podsandboxes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PodSandbox
            properties:
              arch:
                type: string
              className:
                description: ClassName selects the operator-defined runner and network
                  integration.
                minLength: 1
                type: string
              memoryMiB:
                format: int64
                minimum: 128
                type: integer
              sandboxID:
                description: SandboxID is the identity assigned by containerd/Kata.
                minLength: 1
                type: string
              userDataSecretRef:
                description: |-
                  UserDataSecretRef contains the CAA-generated cloud-config in the
                  "userdata" key.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              vcpus:
                format: int64
                minimum: 1
                type: integer
              workloadRef:
                description: WorkloadRef identifies the source Pod represented by
                  this sandbox.
                properties:
                  name:
                    description: Name is the workload Pod name in its source cluster.
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the workload Pod namespace in its source
                      cluster.
                    type: string
                  uid:
                    description: UID disambiguates recreated workload Pods when the
                      source reports it.
                    type: string
                required:
                - name
                type: object
            required:
            - className
            - memoryMiB
            - sandboxID
            - userDataSecretRef
            - vcpus
            - workloadRef
            type: object
            x-kubernetes-validations:
            - message: spec is immutable
              rule: self == oldSelf
          status:
            description: status defines the observed state of PodSandbox
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              ips:
                description: IPs contains addresses assigned to the PodSandbox.
                items:
                  type: string
                type: array
              nodeName:
                description: NodeName is the management-cluster Node hosting the runner.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation reconciled into
                  status.
                format: int64
                type: integer
              runnerRef:
                description: RunnerRef identifies the management-cluster Pod hosting
                  the PodVM.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
