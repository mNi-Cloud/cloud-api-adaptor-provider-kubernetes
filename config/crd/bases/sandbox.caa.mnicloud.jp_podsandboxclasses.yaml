---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: podsandboxclasses.sandbox.caa.mnicloud.jp
spec:
  group: sandbox.caa.mnicloud.jp
  names:
    kind: PodSandboxClass
    listKind: PodSandboxClassList
    plural: podsandboxclasses
    shortNames:
    - psbc
    singular: podsandboxclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PodSandboxClass is the Schema for the podsandboxclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PodSandboxClass
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations are copied to each runner Pod. CNI integrations can be
                  selected using their normal Pod annotations.
                type: object
              kataDir:
                default: /opt/kata
                description: |-
                  KataDir is the host directory containing Cloud Hypervisor and the Kata
                  runtime assets used by the runner.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Labels are copied to each runner Pod. Controller-owned labels take
                  precedence when the same key is present.
                type: object
              memoryOverheadMiB:
                default: 256
                description: MemoryOverheadMiB is reserved in addition to guest memory.
                format: int64
                minimum: 0
                type: integer
              networkMTU:
                default: 1500
                description: NetworkMTU is configured inside the PodVM guest.
                format: int32
                maximum: 9216
                minimum: 576
                type: integer
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector constrains runner Pods to compatible nodes.
                type: object
              podVMImage:
                description: PodVMImage is an OCI image that exports disk.qcow2 into
                  /output.
                minLength: 1
                type: string
              runnerImage:
                description: RunnerImage contains the PodVM launcher.
                minLength: 1
                type: string
              startupGraceSeconds:
                default: 30
                description: |-
                  StartupGraceSeconds delays readiness until guest initialization has
                  completed after Cloud Hypervisor starts.
                format: int32
                maximum: 300
                minimum: 0
                type: integer
              tolerations:
                description: Tolerations allow runner Pods to use dedicated virtualization
                  nodes.
                items:
                  description: |-
                    The pod this Toleration is attached to tolerates any taint that matches
                    the triple <key,value,effect> using the matching operator <operator>.
                  properties:
                    effect:
                      description: |-
                        Effect indicates the taint effect to match. Empty means match all taint effects.
                        When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
                      type: string
                    key:
                      description: |-
                        Key is the taint key that the toleration applies to. Empty means match all taint keys.
                        If the key is empty, operator must be Exists; this combination means to match all values and all keys.
                      type: string
                    operator:
                      description: |-
                        Operator represents a key's relationship to the value.
                        Valid operators are Exists, Equal, Lt, and Gt. Defaults to Equal.
                        Exists is equivalent to wildcard for value, so that a pod can
                        tolerate all taints of a particular category.
                        Lt and Gt perform numeric comparisons (requires feature gate TaintTolerationComparisonOperators).
                      type: string
                    tolerationSeconds:
                      description: |-
                        TolerationSeconds represents the period of time the toleration (which must be
                        of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
                        it is not set, which means tolerate the taint forever (do not evict). Zero and
                        negative values will be treated as 0 (evict immediately) by the system.
                      format: int64
                      type: integer
                    value:
                      description: |-
                        Value is the taint value the toleration matches to.
                        If the operator is Exists, the value should be empty, otherwise just a regular string.
                      type: string
                  type: object
                type: array
            required:
            - podVMImage
            - runnerImage
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
